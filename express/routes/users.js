var express = require('express');
var router = express.Router();

const { sequelize } = require('../models');

/** GET recorded user names */
router.get('/', async function(req, res, next) {
    
});

module.exports = router;
